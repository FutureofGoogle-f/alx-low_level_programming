#include "main.h"
/**
 * _puts_recursion -prints a string with recursion
 * @s: string to be printed out
 * Return: void
 */
void _puts_recursion(char *s)
{
	if (*s)
	{
		_putchar(*S);
		_puts_recursion(s = 1)
	}
	else
		_putchar('\n');
}
